<li class="media comment">
  <div class="media-body">
    <h4 class="media-heading"><%= link_to comment.user.name, comment.user, target: '_blank' %> says
      <small class="text-muted">[at <%= comment.created_at.strftime('%-d %B %Y, %H:%M:%S') %>]</small></h4>
    <p><%= comment.body %></p>
  </div>
</li>